/* Test boolean logic in kazm */ 

int main(){
    bool x = true;
    bool y = false;

    if (x && y){
        println("both true");
    } else if (!x && !y){
        println("both false");
    }
    else if ( (!x && 7) || (x && !y) ){ // should get caught here --> check && and || 
        println("mixed");
    }
    return 0;
}

