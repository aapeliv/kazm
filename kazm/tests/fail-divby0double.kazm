// divide double by 0 and print result

int main() {
  double x;
  double y;
  double z;
  y = 3.14;
  z = y/x;
  // This returns int if x is initialized to be 0.000000
  double_println(z);
  // This expr can be semantically checked. The denominator is simply 0.0 instead of a variable.
  y / 0.0;
}
