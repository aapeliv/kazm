int fun(int a)
{
  return 2;
}

int main()
{
  int i;
  bool b;
  
  b = true;
  i = fun(1);

  return 0;
}

// when I have return 1; in int fun(int a) it is fine
// the problem is when I have return a; 
// I guess this is because we can assign but not use values 